<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <ul>
        <%= link_to "Home", root_path %><span style="margin-left:20px; margin-right:20px;">
        <% if user_signed_in? %>
          <%= link_to "Create An Event", new_event_path %><span style="margin-left:20px; margin-right:20px;">
          <%= link_to "User Page", user_path(current_user) %><span style="margin-left:20px; margin-right:20px;">
          <%= link_to "Log Out", destroy_user_session_path, data: { "turbo-method": :delete } %>
        <% else %>
          <%= link_to "Log In", new_user_session_path %><span style="margin-left:20px; margin-right:20px;">
          <%= link_to "Sign Up", new_user_registration_path %>
        <% end %>
      </ul>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <br><br>
     <%= debug(params) if Rails.env.development? %>
  </body>
</html>
